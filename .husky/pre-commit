#!/usr/bin/env sh

# Block committing changes to README.md
if git diff --cached --name-only | grep -iE '^README\.md$' >/dev/null; then
  echo "Error: README.md is protected. Commit aborted." >&2
  exit 1
fi

exit 0

