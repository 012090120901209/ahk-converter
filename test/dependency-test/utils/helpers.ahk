#Requires AutoHotkey v2.0
; Helper utilities

class Helper {
    static Initialize() {
        MsgBox("Helpers initialized")
    }

    static FormatString(str) {
        return Trim(str)
    }
}
